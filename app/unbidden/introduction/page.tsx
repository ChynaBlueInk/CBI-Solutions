import UnbiddenIntroduction from "@/app/components/unbidden/UnbiddenIntroduction"

export const metadata = {
  title: "Before You Begin | Unbidden",
}

export default function UnbiddenIntroductionPage() {
  return <UnbiddenIntroduction />
}
